# 🚀 Council-AI: Presentation Materials

> **Target Audience:** Hackathon Judges & Developers  
> **Tone:** Professional, Passionate, Technical yet Accessible  
> **Key Message:** "Automating Student Council Operations with Domain-Specific RAG"

---

## 1. 📢 서비스 개요 (Service Overview)

### **"Council-AI, 학생회 전용 RAG 구축을 통해 반복 업무를 0으로 만들다"**

- **Problem:**
  - 학생회는 매년 반복되는 **'문서의 늪'**에 빠져 있습니다.
  - 회의 때마다 안건지/속기록/결과지 3중 문서 작업 발생.
  - 1년치 자료가 파편화되어 다음 대 학생회로 **인수인계가 불가능**.
  
- **Solution:**
  - **Single Source of Truth:** 구글 닥스(Google Docs) 하나로 모든 업무 처리.
  - **Active RAG:** 단순 검색을 넘어, AI가 회의록 작성부터 캘린더 등록, 인수인계서까지 **능동적으로 수행**.

---

## 2. 🛠 기술 스택 & 시스템 아키텍처 (Tech Stack & Architecture)

### **Technology Stack**

| Layer | Stack | Description |
| :--- | :--- | :--- |
| **Frontend** | **Google Apps Script (GAS)** | Serverless UI integrated directly into Google Docs Sidebar. |
| **Backend** | **FastAPI** | High-performance async Python framework. |
| **Async Queue** | **Celery + Redis** | Handling heavy RAG pipelines (ingestion, parsing) asynchronously. |
| **AI Parsing** | **Upstage Doc Parser** | Identifying complex tables and document structures. |
| **LLM & Vision** | **Gemini 1.5 Flash** | Multimodal analysis (Text + Image Captioning). |
| **Embedding** | **Vertex AI** | `text-embedding-004` (768d) for semantic search. |
| **Database** | **PostgreSQL + pgvector** | Hybrid storage for relational data and vector embeddings. |
| **Infra** | **GCP (Compute Engine)** | Dockerized deployment with Application Default Credentials (ADC). |

### 🎨 **System Architecture Image Prompt (Nano Banana)**

```text
Modern, clean, isometric server architecture diagram, tech illustration style.
Central hub is a Python FastAPI Backend logo.
Left side: User interacting with Google Docs (blue icon) and Sidebar UI (Google Apps Script logo).
Arrows flow from Google Docs to FastAPI Backend.
Right side: Data pipeline processing.
1. Upstage AI logo (Scanning document).
2. Gemini AI logo (Brain icon).
3. PostgreSQL database cylinder with Vector icon (pgvector).
4. Redis cube and Celery worker gears (Async processing).
Background: Circuit board pattern, soft blue and purple gradient.
Connecting lines showing data flow: Ingestion -> Parsing -> Embedding -> Storage.
High quality, 3D render, white background.
```

---

## 3. 🧠 RAG 구조 및 연결 (Core Logic)

단순한 RAG가 아닙니다. **"행사(Event)"라는 맥락을 이해하는 RAG**입니다.

### **1. Hierarchical Chunking (계층적 청킹)**
우리는 문서를 단순히 자르지 않고, **'의미 단위'**로 구조화합니다.

- **Parent Chunk (Context):**
  - 안건 하나를 통째로 포함 (예: `## 1. 대동제 가수 섭외 건`).
  - LLM에게 **충분한 맥락(Context Window)**을 제공하여 할루시네이션 방지.
- **Child Chunk (Search):**
  - Parent를 500자 단위로 세분화.
  - **정밀한 검색(Vector Similarity)**을 위해 사용.
  - *검색은 Child로 하고, 답변은 Parent를 참조하여 생성.*

### **2. Event-Driven N:M Structure**
문서를 파일 단위로 관리하지 않고, **행사(Event) 중심**으로 묶습니다.

- **Problem:** "작년 축제 기획안 찾아줘"라고 하면, 기획안 파일만 나오고 당시 회의록(디테일)은 누락됨.
- **Solution:** [Event](file:///c:/Users/imtae/madcamp/2025Winter_4th/backend/app/models/event.py#28-145) 테이블을 중심으로 [Document](file:///c:/Users/imtae/madcamp/2025Winter_4th/backend/app/models/document.py#62-174)(기획안, 결과지, 영수증)와 `Chunk`(회의 내용)를 **N:M**으로 연결.
- **Result:** "축제"를 검색하면 기획안뿐만 아니라, **"당시 회의에서 가수는 누구로 결정했는지"** (Chunk)까지 한 번에 조회.

---

## 4. 🚀 핵심 기능 및 기술 (Key Features)

### **1. 결과지(회의록) 자동 생성 (Smart Minutes)**
- **Description:** 안건지(Template)와 속기록(Raw Voice)을 결합하여 최종 결과지 생성.
- **Tech:** `Upstage Parser`로 안건지 표 구조 인식 → [Gemini](file:///c:/Users/imtae/madcamp/2025Winter_4th/backend/app/services/ai/gemini.py#15-406)가 속기록에서 결정 사항 추출 → `Template Mapping`.

### **2. 캘린더 일정(Todo) 자동 등록**
- **Description:** 회의 중 나온 "누구 님 이거 언제까지 해주세요"를 감지하여 캘린더에 자동 등록.
- **Tech:** [Gemini](file:///c:/Users/imtae/madcamp/2025Winter_4th/backend/app/services/ai/gemini.py#15-406)가 자연어 대화에서 **Action Item(What, Who, When)** 추출 → `Google Calendar API` Insert.

### **3. 인수인계서 자동 제작 (Handover Gen)**
- **Description:** "2025년 총결산 인수인계서 만들어줘" 버튼 하나로 끝.
- **Tech:** 
  - 특정 연도(`2025`)의 모든 [Event](file:///c:/Users/imtae/madcamp/2025Winter_4th/backend/app/models/event.py#28-145) 조회.
  - 각 Event에 연결된 **모든 문서와 회의록 Chunk**를 Aggregation.
  - AI가 **"잘한 점", "아쉬운 점", "내년 유의사항"**을 분석하여 리포트 생성.

### **4. 실시간 질의응답 (Council Chat)**
- **Description:** "재심의 규정이 뭐야?" 같은 학생회칙 질문 즉답.
- **Tech:** `pgvector` HNSW Indexing을 통한 0.1초 이내 초고속 검색 → LLM 답변 생성.

---

## 5. ⚡️ GAS (Google Apps Script) 도입 이유

왜 React가 아닌 **GAS**인가?

### **1. Zero-Friction User Experience**
- 별도의 웹사이트에 접속할 필요가 없습니다. 
- 학생회 임원들이 **이미 사용 중인 Google Docs** 옆에 자연스럽게 녹아듭니다 (Sidebar).

### **2. Native Integration**
- 복잡한 OAuth 2.0 토큰 관리 없이, `DocumentApp.getActiveDocument()` 한 줄로 현재 문서를 제어합니다.
- `CalendarApp`, `DriveApp` 등 구글 생태계 리소스에 **가장 빠르고 안전하게 접근**합니다.

### **3. Serverless Frontend**
- 프론트엔드 배포 서버(Vercel, AWS S3)가 필요 없습니다.
- 구글 서버에서 직접 실행되므로 **유지보수 비용 0원**.

---

## 6. 📂 프로젝트 구조 (File Tree)

```bash
Council-AI
├── backend
│   ├── app
│   │   ├── api              # FastAPI Rotuers
│   │   ├── core             # Config & Security
│   │   ├── models           # SQLAlchemy Models (Event, Document, Chunk)
│   │   ├── pipeline         # 7-Step RAG Pipeline (Ingest->Upload->Embed)
│   │   ├── services         # Gemini, Upstage, Google API Wrappers
│   │   └── tasks            # Celery Tasks (Async Workers)
│   └── tests                # Unit & Integration Tests
├── frontend
│   └── src
│       ├── Code.gs          # GAS Server-side Logic
│       ├── Sidebar.html     # Main UI Entry Point (React-like component)
│       └── Scripts.html     # Client-side Logic (State Management)
└── docs                     # Architecture & API Documentation
```
